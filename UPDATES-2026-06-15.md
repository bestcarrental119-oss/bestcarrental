# 更新内容（2026-06-15）越境配車（异地调车 / 加盟店送車）対応

## 重要：フォルダの版について
アップロードされた ZIP（runOfFleet / banners API / owner予約割当 / tests を含む新しい完全版）を、選択フォルダ「BEST CAR」に反映しました。これまで一部機能が「動かない／見えない」状態だったのは、BEST CAR フォルダが古いサブセット版だったためです。最新版に置き換わったことで、既存実装（⊗キャンセル、国籍選択、免許番号撤去＋IDP任意化＋返金不可注意書き、マイページ証明書類、編集可能な静的ページ、5言語対応 等）が有効になっています。

## 今回の新規実装：越境配車システム
現地在庫が無い空港・エリアでも、近隣加盟店から「おまかせ（class-based）」車両を送車する導線を、フロント〜予約〜バックエンドまで通しで実装。

- 検索結果に加盟店「おまかせ」在庫を統合（`MapSearch.jsx`）。現車は run-of-fleet 在庫を除外し重複表示を防止。
- スマートバナー（黒×金）：現地在庫ゼロかつ加盟店送車が在庫ありの時に「本地库存已售罄…」を結果上部に表示。
- タグで区別：現車＝🟢「機場/門店現車」、加盟店車＝🤝「加盟店送車」＋「最速 X 時間で配送」。
- 料金の透明化：車両カードと予約料金内訳に「異地送車服務費（加盟商提供）」を分離表示。CDW等の公式同等保証・送車案内も明記。
- 予約完了画面に動的な受取案内（送車ドライバーが15分前に指定地点で交接）。
- 加盟店オーナーのバックエンドで料金を設定・変更：
  - 車両フォームに越境配車トグル＋送車料金/所要時間（`OwnerDashboard.jsx`）。
  - 新タブ「🚦 おまかせ管理」に「お任せブース設定」（クラス・料金・送車料金を一覧編集、`/api/vehicles/[id]` PATCH で holder.runOfFleet を更新）と「車両割り当て管理」パネル（pending_assignment 予約に車両を割当）。
- データモデル：`runOfFleet.js` に deliveryFee / deliveryHours を追加。予約API（`reservations/route.js`）が class_based を pending_assignment として扱うよう対応。
- デモ用シード：KIX（関西）・CTS（新千歳）に加盟店送車車両を追加。既存車に空港（HND/NRT）を付与し、空港カード検索が機能するように。
- 多言語：越境配車の全UI文言を 日/英/簡体中文/繁體中文/韓国語 の5言語に追加。

## 不具合修正
- `getHeroBanners` / `saveHeroBanners` が `lib/kv.js` に未定義でビルド警告＋banners API実行時エラー → 追加実装し `INIT_HERO_BANNERS` を `data.js` に定義。

## 検証
- `next build`：✅ 成功（エラー0・警告0、全38ルート生成）。
- run-of-fleet テスト（`test/run-of-fleet.test.mjs`）：✅ 8/8 パス（着手前は 5/8）。

## 追加対応：モバイル対応（スマホ最適化）
PC表示は変えずに、スマホ幅（`sm:`/`md:` ブレークポイント）でのみ最適化。
- 検索ページ（`MapSearch`）：PCは地図とリストの左右分割を維持、スマホは縦積み（リスト上＝`max-h-[34vh]`、地図下＝`h-[56vh]`）。カード余白・画像も縮小。
- 予約モーダル（`Shared`/`BookingModal`）：PCの横長モーダル維持、スマホは下部から全画面（`max-h-[100dvh]`・角丸なし）＋フォーム1列化。
- 管理者・加盟店バックエンド（`AdminApp`/`OwnerDashboard`）：PCはサイドバー/横並びタブ、スマホは上部モバイルナビ＋横スクロールタブ（`min-w-max`）に切替。
- ホーム（`FrontendApp`）：スマホはヒーローバナー全体表示（`object-contain`で見切れなし）＋画面下に固定の主要ナビ（ホーム/検索/マイページ、セーフエリア対応）。

検証：`next build` ✅ 成功、`test/mobile-responsive.test.mjs` ✅ 4/4 パス。

## 既知の残課題（本リクエスト範囲外）
同梱テスト全体は 22/59 パス（着手前 18）。残りは Apple Pay/Google Pay、ヒーローバナーのアップロード/並替UI、予約ID衝突耐性、チャット翻訳origin、anime UI 等、本ビルド版に元々未達の別機能群です。必要であれば次段階で対応します。
