# 更新内容まとめ（2026-06-12）

`npm install` → `npm run dev` で起動。**Supabase / Upstash / Stripe の環境変数が無くても全機能がデモモードで動作します**（設定すれば本番モードに自動切替）。

## ご依頼項目の対応

| # | 項目 | 実装内容 |
|---|------|----------|
| 1 | ログイン bot検証 | スライダーCAPTCHA（自前実装・外部キー不要）+ ハニーポット + 最小操作時間チェック。`components/Captcha.jsx` |
| 2 | 電話・メール検証 | 国番号選択付き電話番号の厳密検証、メール形式検証、パスワード強度検証（`lib/validation.js`）。**メールOTP**: Supabase設定時は登録後に6桁コード入力画面→`verifyOtp`で認証。再送信ボタン付き |
| 3 | 検索/車型/Location | 検索ページに並び替えドロップダウン追加、ホームのLocation入力が実際に絞り込みに反映（空港名でもヒット）、Location候補のサジェスト（datalist）、車型フィルタはタグ検索にも対応 |
| 4 | カレンダー確認ボタン | 予約Step1に「✓ 日付を確定する」ボタンを追加。確定するまで次へ進めず、確定後は「変更」で再編集可能 |
| 5 | 予約ページ ⊗ ボタン | 予約モーダル右上に ⊗ キャンセルボタン（入力途中なら確認ダイアログ）。認証モーダルにも追加 |
| 6 | 車型画像の見切れ | `object-cover`（切り抜き）→ `object-contain`（全体表示）に変更。車両カード/検索リスト/地図ポップアップ/予約確認/マイページすべて対応 |
| 8 | Nationality / 免許番号 | 60カ国以上の国リスト（4言語表示・国旗付き）から選択（`lib/countries.js`）。**免許証番号の入力欄は削除**し、「受取時に原本提示」の案内+所持確認チェックボックスに変更 |
| 9 | 規約・プライバシー・問い合わせ | フッターリンクから動作するページを実装。**管理画面の「📄 サイトページ編集」タブでCMS編集可能**（プレビュー付き、`/api/pages`で永続化）。お問い合わせフォームは送信→管理画面「📥 お問い合わせ受信箱」で閲覧・返信・削除 |
| 12 | マイページ IDP等 | 「🪪 証明書類」タブを新設: 免許（発行国・有効期限）、IDP（番号・発行国・期限）、パスポート（番号・期限）。期限切れ警告付き。Supabase user_metadata にも保存 |
| - | 乗り捨て（異地還車） | 車両カード・検索リスト・地図ポップアップ・おすすめ(おまかせ)枠に「↗️ 乗り捨てOK」バッジ表示。予約Step1で返却場所を空港から選択可（空港ごとの料金も表示・加算） |
| - | 管理/オーナー backend | ダッシュボードに**要対応アラート**（保留予約・車両審査待ち・加盟店申請・未読問い合わせ→クリックで該当タブへ）、今月売上カード、予約の確定/完了/キャンセルのワンクリック操作、ユーザー名未設定でのクラッシュ修正 |
| - | 多言語 | **日本語を追加**し 日/英/簡体中文/繁體中文/韓国語 の5言語に。フロント側ナビにも言語切替を設置、選択はlocalStorageに保存・ブラウザ言語を自動検出。新機能も全て翻訳済み |
| - | 無反応ボタン等の修正 | env未設定時のビルド失敗を修正（`lib/supabase.js`）、Supabase未設定でもログイン/問い合わせ/ページ編集が動くデモフォールバック（`lib/kv.js` メモリストア）、Location検索の未反映を修正、重複していた `components/components/` を削除 |

## 新規ファイル
- `components/Captcha.jsx` — スライダーCAPTCHA + ハニーポット
- `components/StaticPages.jsx` — プライバシー/規約/問い合わせページ
- `lib/countries.js` / `lib/validation.js`
- `app/api/pages/route.js` / `app/api/contact/route.js`

## 環境変数（任意 — 未設定でもデモモードで動作）
```
NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY
UPSTASH_REDIS_REST_URL / UPSTASH_REDIS_REST_TOKEN   ← ページCMS・問い合わせの永続化
STRIPE_SECRET_KEY / NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
```
※ メールOTPはSupabaseダッシュボードで「Email confirmation」を有効にすると動作します。

## 検証済み
`next build` 成功 / 本番サーバー起動 / トップページ200 / ページCMS保存→反映 / 問い合わせ送信→受信箱表示 すべて確認済み。
