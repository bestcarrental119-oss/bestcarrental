# Best Car Rental — 変更点まとめ（2026-06 更新）

対象プロジェクト: Next.js 14 + Supabase + Stripe + Upstash Redis 製のレンタカー予約サイト。
今回の修正は、ご依頼の4グループ（予約フロー修正 / 跨区域送车 / 認証・本人情報 / 管理者バックエンド）を中心に実装しました。
※ 既存の i18n（en / ja / zh-TW / zh-CN / ko の5言語）とおまかせ（RunOfFleet）機能は元から存在していたため、それを土台に拡張しています。

---

## 1. 予約フロー修正（検索 / 車型 / Location / カレンダー / 予約ページ / 画像 / ボタン）

| 依頼 | 対応 | 変更ファイル |
|---|---|---|
| 車型画像が全体表示されない | 一覧・予約・地図・おまかせの全カードで画像を `object-cover`（切り抜き）→ `object-contain`（全体表示）に変更。背景をグレーにして見切れを解消 | `FrontendApp.jsx` / `BookingModal.jsx` / `MapSearch.jsx` / `RunOfFleetCard.jsx` / `MyPage.jsx` |
| カレンダーに確認ボタン追加 | 日程を選んだ瞬間に自動確定していたのを廃止。開始日・返却日を選ぶと「✓ この日程で確定 / Confirm」ボタンと「クリア」ボタンが出るようにした | `AvailabilityCalendar.jsx` |
| 予約ページに取消（⊗）ボタン追加 | 全モーダル共通の右上に ⊗ クローズボタンを追加。予約モーダルにも自動的に表示される | `Shared.jsx`（Modal） |
| 反応しないボタンの修正 | フッターの「プライバシー / 利用規約 / お問い合わせ」が `<span>`（クリック不可）だったのを `<button>` 化し、内容モーダルを開くように修正（下記4も参照） | `FrontendApp.jsx` |

---

## 2. 跨区域送车（异地调车）機能

「無車で離脱させない」ための一連の機能をフロント・バックエンド双方に実装しました。

- **スマートバナー（黒金スタイル）**: 検索結果で「本地在庫が0、かつ周辺加盟店に車あり」を検知したときだけ、ページ上部に
  「💡 本地在庫は完売 — 加盟店の跨区域送車を開始しました…」の案内を表示。`MapSearch.jsx`
- **タグで明確に区別**:
  - 本地車両 → 緑タグ「✅ 空港/店舗 現車」
  - 加盟店車両 → 金タグ「🚚 加盟店送車 ・ X時間前までに予約」
  - 異地還車可 → 紫タグ「↔ 乗り捨て（異地返却）可」
  一覧カード・地図リストカード・おまかせカードに表示。`FrontendApp.jsx` / `MapSearch.jsx` / `RunOfFleetCard.jsx`
- **送達時効の明示**: 加盟店車両に「⏱ 最短 X 時間で送達」を表示。
- **費用透明化**: 予約の料金内訳（Step4）で「車両レンタル料」と「跨区域送車サービス費（周辺加盟店が提供）」を分離表示。心理的ハードルを下げる説明文も追加。`BookingModal.jsx`
- **履約保障の文言**: 加盟店車両の予約時に「すべての加盟店車両は BEST 公式の安全点検を通過し、同等の公式補償・CDW が付帯します」を表示。
- **加盟オーナー/管理者が料金設定**: 車両編集フォームに「跨区域送车」トグル＋「送車サービス費(¥)」「最短送達時間(時間)」を追加。価格をバックエンドで設定・変更可能に。`AdminApp.jsx`（VehicleForm）
- **データモデル拡張**: 車両に `deliveryAvailable / deliveryFee / deliveryEtaHours / oneWayReturn` を追加。デモ用に HiAce（id:4）を加盟店送車対象として設定。`data.js`

> 補足: 「主站点在庫=0のとき自動で加盟店站点を検索する在庫共有ロジック」「距離/区域運費表による自動計費」は、現状フロント側の判定（本地0＆加盟店ありでバナー＋タグ）と、車両ごとの固定送車費設定までを実装しています。站点間の距離テーブルに基づく完全自動計費は、加盟店站点マスタ（住所・座標）のデータ設計が前提となるため次段階の作業として残しています（下記「残作業」参照）。

---

## 3. 認証・本人情報

- **防机器人验证（CAPTCHA）**: ログイン/新規登録に簡易ボット対策（足し算チャレンジ）を追加。外部キー不要で動作。`AuthModal.jsx`
- **手机号 / 邮箱 验证**: 登録時に電話番号フィールドを追加し、メール・電話の形式バリデーションを実装。Supabase のメール確認が有効な場合は確認メール送付フローに対応。`AuthModal.jsx`
- **Nationality 国家选择**: 予約フォーム・マイページの国籍をテキスト入力 → 国選択ドロップダウン（24カ国＋その他、多言語ラベル）に変更。`COUNTRIES` を `data.js` に新設。
- **Driver License 番号不要化**: 予約フォームの運転免許証番号入力欄を廃止。
- **IDP 任意化 ＋ 注意書き**: 予約時の IDP アップロード必須を解除。代わりに
  「IDPを発行できなかった/受取時に不備があった場合のキャンセルは返金できません」
  という注意書きを5言語で表示。`BookingModal.jsx`
- **マイページに本人確認書類タブ**: 「🪪 本人確認書類」タブを新設。運転免許証・IDP・パスポートの有効期限＋ファイル（画像/PDF, 3MBまで）を登録・保存可能（Supabase ユーザーメタデータに保存）。`MyPage.jsx`

---

## 4. 管理者バックエンド（法的ページ編集など）

- **法的ページの編集機能**: 管理画面に「📄 法的ページ・お問い合わせ」タブを新設。プライバシーポリシー / 利用規約 / お問い合わせを **言語ごと** に編集・保存可能。`AdminApp.jsx`（LegalPagesEditor）
- **保存基盤**: `/api/legal`（GET/POST）と Upstash Redis 保存関数 `getLegalPages / saveLegalPages` を追加。初期文面は5言語で同梱（`INIT_LEGAL_PAGES`）。`app/api/legal/route.js` / `lib/kv.js` / `lib/data.js`
- **フロント反映**: フッターのリンクをクリックすると、その言語の最新内容がモーダルで表示される。アプリ起動時に `/api/legal` から読み込み。`context.jsx` / `FrontendApp.jsx`
- **加盟オーナー向け**: 車両編集での跨区域送車料金・異地還車設定（上記2）も加盟オーナーのバックエンドで使えます。

---

## 多言語対応
新規追加した文言はすべて 簡体字 / 繁体字 / 英語 / 韓国語 / 日本語 の5言語で `lib/i18n.js` の `extraTranslations` に追加済みです。

## 検証
- 編集した全ファイル（14ファイル）を esbuild で構文チェック → エラーなし。
- ※ 完全な本番ビルド（`npm install && npm run build`）と実機での動作確認は、Supabase / Stripe / Upstash の環境変数（`.env.local`）を設定の上、お手元で実行してください。

---

## 主な変更ファイル一覧
- `lib/data.js` — COUNTRIES / countryLabel / INIT_LEGAL_PAGES 追加、デモ車両に送車設定
- `lib/i18n.js` — extraTranslations（5言語）追加、t() のフォールバック拡張
- `lib/kv.js` — legalPages の保存/取得、getAllData に追加
- `lib/context.jsx` — legalPages を state/HYDRATE/dispatch に追加、起動時ロード
- `components/Shared.jsx` — Modal に ⊗ クローズボタン
- `components/FrontendApp.jsx` — 画像全体表示、本地/加盟店/異地還車タグ、フッター法的ページモーダル
- `components/BookingModal.jsx` — 国籍ドロップダウン、免許番号欄削除、IDP任意化＋注意書き、送車費内訳
- `components/AvailabilityCalendar.jsx` — 確認/クリアボタン
- `components/MapSearch.jsx` — スマートバナー、タグ、画像全体表示
- `components/RunOfFleetCard.jsx` — おまかせに異地還車タグ、画像全体表示
- `components/AuthModal.jsx` — CAPTCHA、電話番号、形式バリデーション
- `components/MyPage.jsx` — 本人確認書類タブ、国籍表示、画像全体表示
- `components/AdminApp.jsx` — 法的ページ編集タブ、車両に跨区域送車設定
- `app/api/legal/route.js` — 新規 API

## 残作業（次段階の推奨）
1. 加盟店站点マスタ（住所・座標）と、站点間距離テーブルに基づく **送車費の完全自動計算**。
2. 主站点在庫0時に関連站点在庫を **自動検索** する在庫共有ロジックの DB 実装（Supabase スキーマ拡張）。
3. 電話番号の **SMS OTP 認証**（Twilio 等の SMS プロバイダ連携が必要）。
4. 予約成功メール/確認ページでの「送車ドライバー名・15分前交接」などの動的取車指引テンプレート。
5. `components/components/`（旧コピーと思われる重複ディレクトリ）の整理。今回は未使用のため未変更。
