# Best Car Rental — 今回の追加・修正まとめ

ご依頼の項目を `Best-CarRental-globalchat` をベースに実装しました。構文チェック（esbuild bundle）は全変更ファイルでエラーなしです。

---

## 1. オーナー／管理者バックエンドの多言語対応（英・中・日ほか）
- 管理者バックエンド（AdminApp）は**元から i18n 対応済み**（言語切替・ラベル `t()` 化済み）であることを確認。
- オーナー管理画面（OwnerDashboard）に **言語切替ボタンを追加**。タブ（車両管理／お任せ／予約一覧／カレンダー／チャット／アドオン／異地返却／売上／設定）と承認バッジを5言語（日・英・簡中・繁中・韓）対応に。
- `lib/i18n.js` の `extraTranslations` に新規キーを5言語で追加。
- ※オーナー画面の**個別フォーム内の細かな文言**（車両編集フォームの各入力ラベル等）は分量が非常に多いため、今回は主要ナビ・見出し・ボタンを優先対応。残りは次段階で同じ仕組みで追加可能です。

## 2. フロントエンドの空港検索を上部に
- トップページの**ヒーロー内の検索ボックス直下**に「✈️ 空港から探す」のクイック選択バー（横スクロール）を追加。サイトを開いてすぐ各日本の国際空港を選べます。
- 既存の下部の空港セクションも残しています。`components/FrontendApp.jsx`

## 3. 管理者：登録済み車両の価格変更
- 管理バックエンドの車両一覧 → 「編集」→ 価格（日額／時間額）を変更して保存できることを確認（`upsert` で更新）。**追加実装不要で利用可能**。`components/AdminApp.jsx`

## 4. 為替レート＋選んだ通貨での価格表示
- ナビに**通貨セレクター**（JPY/USD/EUR/CNY/KRW/TWD/HKD/GBP/AUD/THB/SGD）を追加。
- 車両カード・予約モーダル（料金内訳／決済）に「≈ 選択通貨の概算額」を表示。
- レートは無料FX API（`/api/fx`、base=JPY）から取得、取得失敗時は内蔵の概算レートにフォールバック。決済自体は従来どおりJPYです。
- 新規: `lib/currency.jsx` / `components/CurrencySwitcher.jsx` / `app/api/fx/route.js`

## 5. クレカ情報の保存＆次回自動入力
- 決済時に「このカードを次回のために保存」チェックを追加。次回予約では**保存済みカードを選ぶだけ**で決済（手入力不要）。
- Stripe の Customer＋`setup_future_usage` でカードを保存し、保存カードは**オフセッション決済**で確定。下4桁・ブランドのみ保持（生のカード番号は保存しません）。
- マイページのプロフィールから保存カードの削除も可能。
- 新規: `app/api/cards/route.js`、`lib/kv.js` にカード保存ヘルパー、`app/api/payments/route.js` を拡張。
- ※実際の課金には Stripe のキー設定が必要です。未設定時はデモ動作（課金なしでフロー確認可）。

## 6. 電話番号の国番号を先に選択
- 共通コンポーネント `components/CountryPhoneInput.jsx`（国番号セレクト→番号入力、20か国超）を新設。
- **新規登録（AuthModal）／予約フォーム／マイページのプロフィール**で使用。後から連絡が取れるよう国番号付きで保存。

## 7. ゲストのプロフィール編集（電話番号など）／IDP再アップ
- マイページの「プロフィール」タブを**編集可能化**（氏名・電話番号〔国番号付き〕・国籍）。Supabase のユーザーメタデータに保存。
- IDP（国際免許）など本人確認書類の**再アップロード**は既存の「🪪 本人確認書類」タブで対応済み（有効期限更新・ファイル差し替え可）。

## 8. 予約日程：返却は出発より後のみ
- 予約モーダルの日時入力で、返却の選択可能範囲を**出発日時より後に制限**（`min` 設定＋出発未選択時は返却を無効化）。出発を後ろにずらして返却が過去になる場合は自動でクリア。`components/BookingModal.jsx`

## 9. オーナー管理画面の配色変更
- オーナー画面ヘッダーに**配色セレクター**（ダーク／ライト／スレート／ネイビー）を追加。明るい配色も選べます。選択はブラウザに保存。`components/OwnerAppearance.jsx`

## 10.（ご質問）借り手⇔オーナーのチャット機能
- **有効です。** 既に実装済み（Supabase の `conversations`/`messages`、`/api/chat`、自動翻訳付き、未読バッジ）。
  - 借り手側: 車両カードやマイページから連絡可能。
  - オーナー側: 管理画面の「チャット」タブ。
  - ※動作には Supabase の環境変数設定が必要です。

---

## 11. 淘宝/拼多多式「購入前に質問」＋通知強化（追加対応）
- **購入前に質問CTA**: 車両カードのチャットボタンを「💬 質問（咨询/諮詢/문의/Ask）」と明示した目立つボタンに変更。未登録なら登録 → その車両のチャットへ（＝登録を促しつつ事前質問できる淘宝式）。`components/FrontendApp.jsx`
- **アプリ内通知の強化（A）**: 新着メッセージで**アプリ内トースト**を表示（OS通知が未許可でも気づける）。Supabase Realtimeの購読で**即時**反映＋未読バッジ更新。借り手・オーナー両方に対応。ポーリングも15秒に短縮。`components/FrontendApp.jsx` / `components/OwnerDashboard.jsx`
- **メール通知（B）**: チャット送信時、受信者へ新着メールを送信（オフラインでも気づける）。同一会話は10分に1回までに制限（スパム防止）。`lib/email.js`（Resend利用、`RESEND_API_KEY`未設定時はno-op）/ `app/api/chat/[id]/route.js` / `lib/kv.js`（レート制限ロック）
  - 必要な環境変数: `RESEND_API_KEY`、`NOTIFY_EMAIL_FROM`（検証済み送信元）、任意で `NEXT_PUBLIC_APP_URL`（メール内リンク用）。

## 主な新規／変更ファイル
- 新規: `lib/currency.jsx`, `lib/countries.js`, `components/CurrencySwitcher.jsx`, `components/CountryPhoneInput.jsx`, `components/OwnerAppearance.jsx`, `app/api/fx/route.js`, `app/api/cards/route.js`
- 変更: `lib/i18n.js`, `lib/kv.js`, `app/layout.jsx`, `app/api/payments/route.js`, `components/FrontendApp.jsx`, `components/BookingModal.jsx`, `components/AuthModal.jsx`, `components/MyPage.jsx`, `components/OwnerDashboard.jsx`

## 動作確認について
- 全変更ファイルの構文/依存チェック（esbuild bundle）はエラーなし。
- 完全な本番ビルド（`npm install && npm run build`）と実機確認は、`.env.local` に Supabase / Stripe / Upstash の各キーを設定の上、お手元で実行してください。
