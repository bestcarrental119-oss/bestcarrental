# 今回の追加・変更まとめ（2026-07-25）

6つの依頼をすべて実装しました。内部ID・URL・DBの値は一切変更していないため、既存データ・既存機能はそのまま動作します。

---

## 1. 新規登録時のメールアドレス認証（6桁コード）

- サインアップ時、入力されたメールアドレスに**6桁の認証コード**を送信し、コードが一致した場合のみアカウントを作成します。存在しない/入力ミスのメールでは登録できません。
- コードは **SHA-256でハッシュ化**して保存（生コードは保存しない）。有効期限10分、試行5回まで、再送は60秒間隔。
- 保存先は自動判定：**Upstash Redis** →（無ければ）**Supabase `email_otps`テーブル** →（無ければ）メモリ。
- サービスロールキーがある場合はサーバー側で「確認済み」アカウントを作成するため、**Supabase標準の確認メールと二重にならず**、そのまま自動ログインします。

追加/変更: `lib/otp.js`（新規）, `lib/email.js`（`sendVerificationCode`追加）, `app/api/auth/email-otp/route.js`（新規）, `components/AuthModal.jsx`

## 2. プロフィールからのメールアドレス変更（新メールへコード認証）

- マイページ →「プロフィール」に **📧 メールアドレス** 欄を追加。「変更」→ 新しいアドレス入力 → そのアドレスに6桁コード送信 → 認証成功で変更が適用されます。
- 新アドレス宛のコードを認証できて初めて変更されるため、なりすまし変更を防げます。

変更: `components/MyPage.jsx`（`ChangeEmailSection`追加）, `app/api/auth/email-otp/route.js`（`change_email`対応）

## 3. パスワードを忘れた場合（メールで再設定）

- ログイン画面に「パスワードをお忘れですか？」リンクを追加。メールアドレスを入れると**再設定リンク**が届きます。
- リンク先の **`/reset-password`** ページで新しいパスワードを設定できます。

追加/変更: `components/AuthModal.jsx`（forgot導線）, `app/reset-password/page.jsx`（新規）

## 4. 最高権限管理者（admin）によるアカウント削除（関連データも削除）

- 管理画面のユーザー一覧に「🗑 アカウント削除」を追加（**adminロールのみ表示**、自分自身は削除不可）。確認を挟んで実行します。
- 削除時、そのユーザーの**予約・レビュー**に加え、オーナーの場合は**車両・拠点・片道出品・受渡記録・チャット**も連鎖削除します。規約違反ユーザー・オーナー・ダミーアカウントの整理に使えます。
- サーバー側で**呼び出し元がadminか検証**してから実行します。

変更: `app/api/admin/users/route.js`（`DELETE`追加＋admin検証）, `components/AdminApp.jsx`（削除ボタン）, `lib/i18nOwnerAdmin.js`（全5言語の文言）

## 5. 予約完了直後の「予約確認・受取QR」

- 予約が完了した時点（`paid`/`confirmed`/`pending` や決済 `authorized`）で、**貸出日を待たずに**マイページからQRを表示できるようにしました。オーナーはこのQRを読み取って予約・本人確認ができます。
- ボタン名を「予約確認・受取QR」に明確化（全5言語）。

変更: `components/MyPage.jsx`（表示条件を拡張）, `lib/i18nOwnerAdmin.js`（`pp_button`）

## 6. ブランド名変更（全言語の表示テキスト）

- **Best One-Way / Best One Way → Best Match**
- **Best Go → Best Anywhere**
- 日本語・英語・簡体字・繁体字・韓国語すべての**表示名**を置換。
- **内部ID（`best-go` / `best-one-way`）・URL（`/api/one-way/...`）・DBの値・翻訳キー名は変更していない**ため、既存の予約・出品・配線はそのまま動作します。

変更: `lib/i18n.js`, `lib/i18nOneWay.js`, `lib/oneWay.js`, `components/OwnerDashboard.jsx`, `components/CrossReturnPanel.jsx`, `components/FrontendApp.jsx`, `components/oneway/*.jsx`, `app/api/owner/cross-return/route.js`, `app/api/owner/locations/route.js`

---

## 反映に必要な設定（デプロイ時）

1. **SQLを実行**（Supabase SQL Editor）
   `supabase/migrations/20260725_email_otp_and_account_admin.sql` を実行し、`email_otps` テーブルを作成。
   ※ Upstash Redis を使っている場合はこのテーブルは無くても動きます（フォールバック用）。

2. **メール送信（Resend）**
   既存の `RESEND_API_KEY` / `NOTIFY_EMAIL_FROM` がそのまま認証コード送信にも使われます（`EMAIL_SETUP_メール設定.md` 参照）。未設定だと本番ではコード送信が503になります（開発モードではコンソールにコード出力）。

3. **Supabase 認証設定（推奨）**
   - `SUPABASE_SERVICE_ROLE_KEY` を設定しておくこと（サーバー側でのアカウント作成・メール変更・削除に必要）。
   - 独自の6桁コードで確認するため、Supabase の Authentication → **「Confirm email」はOFF** を推奨（標準の確認メールと二重送信を防ぐため）。
   - パスワード再設定リンクの遷移先に本番URLを許可： Authentication → URL Configuration → **Redirect URLs** に本番URL（例 `https://bestcar-rental.com`）を追加。

4. **環境変数**
   `NEXT_PUBLIC_APP_URL`（または実行時のオリジン）が再設定メールのリンク先に使われます。

---

## 動作確認の目安

- 新規登録 → コード入力画面 → メール受信 → 認証 → 自動ログイン
- プロフィール → メール変更 → 新アドレスにコード → 認証で変更反映
- ログイン画面 → パスワード忘れ → メール → `/reset-password` で更新
- 管理画面（admin） → ユーザー詳細 → 🗑 アカウント削除 → 確認 → 一覧から消える
- 予約完了 → マイページ → 「予約確認・受取QR」が即表示
- 画面表示が Best Match / Best Anywhere になっている（各言語）
